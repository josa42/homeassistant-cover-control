name: CI

on:
  push:
    branches: [main]
  # Every pull request, whatever it targets: work lands on long-running
  # branches before it reaches main, and that is exactly where a broken build
  # needs to show up.
  pull_request:
  # Weekly, Sunday 00:00 UTC: catches upstream Home Assistant breakage without
  # a run a day on a repo that goes quiet for months at a time.
  schedule:
    - cron: "0 0 * * 0"

jobs:
  ci:
    uses: josa42/gha-workflows/.github/workflows/ha-integration.yml@main
